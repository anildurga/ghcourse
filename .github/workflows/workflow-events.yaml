name: workflow-events
on: push

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the Trigger
        run: echo "I'have been triggered by a(n) ${{github.eventname}} event."
      
           
     
    
