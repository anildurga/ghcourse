name: practice
on: push
jobs:
  echo-hello:
     runs-on: ubuntu-latest
     steps:
      - name: say Hello
        run: echo "Hello World"
  echo-goodbye:
      runs-on: ubuntu-latest
      steps:
       - name: Successful step
         run: |
           echo "I will success"
           exit 0
       - name: Say goodbye
         run: echo "Goodbye!"
          
